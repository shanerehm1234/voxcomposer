{
  "name": "@voxcomposer/plugin-generic-http",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vox Composer example plugin — fire an HTTP request at a timestamp.",
  "license": "MIT",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@voxcomposer/plugin-sdk": "workspace:*",
    "@voxcomposer/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
